{
  "name": "whimbrel-root",
  "version": "0.0.6",
  "type": "module",
  "private": true,
  "description": "Automation Engine",
  "license": "ISC",
  "author": "Sven Johansson",
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "packages/cli",
    "packages/core/*",
    "packages/lib/*",
    "packages/facets/*",
    "packages/test/*",
    "integration-tests/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "npm run --workspaces clean --if-present",
    "publish": "npm run --workspaces publish --if-present",
    "test": "npm run --workspaces test --if-present",
    "typecheck": "npm run --workspaces typecheck --if-present"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.6"
  }
}
